<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "urn:fontconfig:fonts.dtd">
<fontconfig>
  <!-- Resolve relative to the repository root used by the build. -->
  <dir prefix="cwd">scripts/fonts</dir>
</fontconfig>
